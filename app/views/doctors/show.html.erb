<h1><%= @doctor.name %></h1>
<p><%= @doctor.department %></p>

<p>Appointments: </p>

<ul>
    <% @doctor.appointments.each do |appointment| %>
        <li>Date and time: <%= appointment.doctor_date %> </li>
        <li>Patient: <%= link_to appointment.patient.name, patient_path(appointment.patient) %></li>
    <% end %>
</ul>
